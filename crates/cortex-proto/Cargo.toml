[package]
name = "cortex-memory-proto"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Protobuf and gRPC definitions for the Cortex graph memory engine"
repository = "https://github.com/MikeSquared-Agency/cortex"

[dependencies]
tonic.workspace = true
prost.workspace = true
prost-types = "0.13"
serde.workspace = true
serde_json.workspace = true

[features]
# Enable this to regenerate src/generated/ from proto/cortex.proto.
# Requires protoc to be installed. Normal builds use the pre-generated file.
regenerate = []

[build-dependencies]
tonic-build = "0.12"
