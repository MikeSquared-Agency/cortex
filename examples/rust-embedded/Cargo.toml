[package]
name = "rust-embedded-example"
version = "0.1.0"
edition = "2021"

[dependencies]
cortex-core = { path = "../../crates/cortex-core" }
anyhow = "1.0"
tokio = { version = "1", features = ["full"] }
